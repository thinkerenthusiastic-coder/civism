services:
  - type: web
    name: civsim
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    disk:
      name: civsim-state
      mountPath: /var/data
      sizeGB: 1
